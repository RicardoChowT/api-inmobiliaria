applications:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    port: 80
    domains:
      - change4canton.com

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    port: 3001
    env:
      - NODE_ENV=production
      - DB_HOST=srv980470.hstgr.cloud
      - DB_USER=Rjchow
      - DB_PASSWORD=Rjchow44/Tz
      - DB_NAME=inmobiliariachowmolina
